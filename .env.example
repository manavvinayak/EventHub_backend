PORT=5000
MONGO_URI=your_mongodb_connection_string

JWT_SECRET=your_jwt_secret_key

# Email Configuration (Required for sending registration confirmations)
# For Gmail: Use your Gmail address and App Password (not regular password)
# For other providers: Update the service in emailService.js
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password-or-smtp-password
FRONTEND_URL=http://localhost:5173

# How to set up Gmail App Password:
# 1. Enable 2-factor authentication on your Gmail account
# 2. Go to Google Account settings > Security > App passwords
# 3. Generate a new app password for "Mail"
# 4. Use that app password in EMAIL_PASS (not your regular Gmail password)